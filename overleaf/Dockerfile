FROM texlive/texlive:latest

WORKDIR /workspace

# Instalar dependências adicionais se necessário no futuro
# RUN apt-get update && apt-get install -y --no-install-recommends \
#     make \
#     && rm -rf /var/lib/apt/lists/*

# CMD removido para permitir controle via docker-compose e Makefile
